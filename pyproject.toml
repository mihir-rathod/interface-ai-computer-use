[project]
name = "interface-ai-computer-use"
version = "0.1.0"
description = "Computer-use automation system: LLM-driven discovery, artifact recording, deterministic replay, and human escalation against a mock legacy bank app."
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.32",
    "jinja2>=3.1",
    "python-multipart>=0.0.12",
    "pydantic>=2.9",
    "playwright>=1.48",
    "google-genai>=0.3",
    "python-dotenv>=1.0",
    "httpx>=0.27",
]

[dependency-groups]
dev = [
    "pytest>=8.3",
    "pytest-asyncio>=0.24",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["agent", "artifacts_lib", "replay", "safety", "escalation", "evidence_lib", "mockbank"]
